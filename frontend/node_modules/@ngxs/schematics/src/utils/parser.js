"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const core_1 = require("@angular-devkit/core");
const schematics_1 = require("@angular-devkit/schematics");
class Parser {
    nameParser(options) {
        const nameWithoutPath = core_1.basename(options.name);
        const namePath = core_1.dirname((options.path === undefined ? '' : options.path)
            .concat('/')
            .concat(options.name));
        return {
            name: nameWithoutPath,
            path: core_1.normalize('/'.concat(namePath))
        };
    }
    specParser(option) {
        return option ? schematics_1.noop() : schematics_1.filter(path => !path.includes('spec'));
    }
}
exports.Parser = Parser;
