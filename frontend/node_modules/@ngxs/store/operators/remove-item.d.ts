import { Predicate } from './internals';
/**
 * @param selector - index or predicate to remove an item from an array by
 */
export declare function removeItem<T>(selector: number | Predicate<T>): (existing: T[]) => T[];
