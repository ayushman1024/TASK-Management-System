import { MetaDataModel, StateClass, StatesByName } from '../internal/internals';
export declare abstract class StoreValidators {
    static stateNameRegex: RegExp;
    static stateNameErrorMessage(name: string): string;
    static checkCorrectStateName(name: string): void;
    static checkStateNameIsUnique(state: StateClass, statesByName: StatesByName): string;
    static getValidStateMeta(state: StateClass): MetaDataModel;
}
